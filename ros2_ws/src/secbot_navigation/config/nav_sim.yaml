# nav_sim.yaml (custom config read by yaml-cpp)
frames:
  map: "map"
  odom: "odom"
  base: "base_link"

topics:
  odom: "/odometry/filtered"
  cmd_vel: "/cmd_vel"
  goal: "/goal_pose"
  path: "/global_path"
  trajectory: "/drive_base/trajectory"

smoothing:
  max_skip: 3

trajectory:
  speed: 1.0
  lookahead_distance: 0.7